apiVersion: v1
kind: ConfigMap

metadata:
  name: prometheus-config
  namespace: quantpulse

data:
  prometheus.yml: |
    global:
      scrape_interval: 5s

    scrape_configs:

      - job_name: "quantpulse-backend"

        metrics_path: /metrics

        static_configs:
          - targets:
              - backend:8000

      - job_name: "quantpulse-ingestion"

        metrics_path: /metrics

        static_configs:
          - targets:
              - ingestion:8001

      - job_name: "kube-state-metrics"

        metrics_path: /metrics

        static_configs:
          - targets:
              - kube-state-metrics:8080

      - job_name: "redis-exporter"

        metrics_path: /metrics

        static_configs:
          - targets:
              - redis:9121