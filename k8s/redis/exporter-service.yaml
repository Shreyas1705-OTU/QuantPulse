apiVersion: v1
kind: Service

metadata:
  name: redis-exporter
  namespace: quantpulse

spec:
  selector:
    app: redis-exporter

  ports:
    - name: http-metrics
      port: 9121
      targetPort: 9121
